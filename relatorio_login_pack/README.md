# Login + Gestão de Usuários

1. `cp .env.example .env` e edite JWT_SECRET.
2. `npm install`
3. `npm start`
4. Abra http://localhost:3000 e crie o primeiro admin (se banco vazio).
5. Seu app fica em public/app.html.
JWT_SECRET=algum_segredo_forte
PORT=10000
